{
  "name": "backend",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn node build/src/index.js",
    "dev": "yarn nodemon src/index.ts",
    "lint": "yarn eslint '**/*.ts'",
    "clean": "yarn rimraf build",
    "prebuild": "yarn clean",
    "build": "yarn prebuild && tsc",
    "fix": "yarn eslint --fix '**/*.ts'"
  },
  "dependencies": {
    "apollo-server": "^3.9.0",
    "graphql": "^16.5.0",
    "node-fetch": "2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/node-fetch": "2",
    "eslint": "^8.18.0",
    "nodemon": "^2.0.18",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  }
}
